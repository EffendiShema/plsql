/* LEFT JOIN: find customers with or without transactions */
SELECT 
    c.customer_id,
    c.customer_name,
    c.region,
    t.transaction_id,
    t.transaction_date,
    t.amount
FROM customers AS c
LEFT JOIN transactions AS t
    ON c.customer_id = t.customer_id
ORDER BY c.customer_id, t.transaction_date;
